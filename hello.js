// This is plain JavaScript — but running on your machine, not in a browser
const name = "Expense Tracker";
const version = 1;

console.log(`Starting ${name} v${version}`);
console.log(`Current time: ${new Date().toISOString()}`);
